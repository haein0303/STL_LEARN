호출가능(callable) 타입(type)
1. 함수포인터
    - 함수의 시작번지를 저장할 수 있다.
2. 람다(lambda) 
    - 이름없는 함수
    ??? - 이름이 없는데 누가 어떻게 불러 쓰는거니?
    sort의 예제
       - sort(반복자 b, 반복자 e, 호출가능타입 f)

3. (function call operator)를 오버로딩한 클래스
    - functor 
4. 맴버함수포인터



호출가능한 타입의 종류? - 무한

function - 어떤 호출가능한 타입이더라도 function 객체로 대표할 수 있다.

